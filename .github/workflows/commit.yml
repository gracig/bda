name: Commit

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch: {}

jobs:
  build:
    uses: gracig/workflows/.github/workflows/main.yml@main
    with:
      commit-workflow: "bbw-rust"
      rust-working-directory: "."
